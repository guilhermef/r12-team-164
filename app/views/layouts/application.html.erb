<!DOCTYPE html>
<html lang='en'>
  <head>
    <title>Real Life Social</title>
    <meta property="og:title" content="Real Life Social" />
    <meta property="og:url" content="http://timehome.r12.railsrumble.com" />
    <meta property="og:image" content="<%= image_url('logo.png')%>" />
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div id="fb-root"></div>
    <div class='navbar navbar-inverse navbar-static-top'>
      <div class='navbar-inner'>
        <div class='container'>
          <a class='brand' href='/'>
            Real Life Social
          </a>
          <div class="pull-right login">
            <% if user_signed_in? %>
              <span>Welcome <%= current_user.name %></span>
              <%= link_to 'my profile', user_url(:me) %>
              <%= link_to 'logout', user_sign_out_url, method: :delete %>
            <% else %>
              <span class='f_logo'></span>
              <%= link_to 'login', '/users/auth/facebook', class: 'button' %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-error"><%= alert %></p>
    <% end %>
    <%= yield %>

    <footer class='footer'>
      <div class='container'>
        Real Life Social is a <a href='http://railsrumble.com/entries/164-timehome'>Timehome</a> production.
        Made in 48 hours or less for the <a href='http://railsrumble.com'>Rails Rumble 2012</a>
      </div>
    </footer>
    <%= yield :javascript_bottom %>
    <script src="http://s0.railsrumble.com/banner.js"></script>
  </body>
</html>
